- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.tmux.conf: .tmux.conf
    ~/.vim: .vim
    ~/.vimrc: .vimrc
    ~/.zshrc: .zshrc
    ~/.vscode: .vscode
    ~/.config/alacritty/alacritty.yml: alacritty.yml

- create:
    - ~/.vim/undo-history
